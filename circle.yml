machine:
  node:
    version: 5.4.1

dependencies:
  override:
    - npm install -g ts-node typescript
    - npm install

test:
  override:
    - npm test

deployment:
  gh-pages:
    branch: master
    commands:
      - npm run deploy



